import puppeteer from "puppeteer";(async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto("http://localhost:5173", {waitUntil: "networkidle0"}); await new Promise(r => setTimeout(r, 2000)); const color = await page.evaluate(() => { const canvas = document.querySelector(".maplibregl-canvas"); if (!canvas) return "no canvas"; const gl = canvas.getContext("webgl") || canvas.getContext("webgl2"); if(!gl) return "no gl"; const pixels = new Uint8Array(4); gl.readPixels(canvas.width/2, canvas.height/2, 1, 1, gl.RGBA, gl.UNSIGNED_BYTE, pixels); return Array.from(pixels); }); console.log("COLOR:", color); await browser.close(); })();

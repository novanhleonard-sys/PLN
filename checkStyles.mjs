import puppeteer from "puppeteer";(async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto("http://localhost:5173", {waitUntil: "networkidle0"}); const canvasStyle = await page.evaluate(() => { const c = document.querySelector(".maplibregl-canvas"); return c ? window.getComputedStyle(c).display : "no canvas"; }); console.log("Canvas display:", canvasStyle); await browser.close(); })();

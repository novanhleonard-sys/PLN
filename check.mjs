import puppeteer from "puppeteer";(async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on("console", msg => console.log("PAGE LOG:", msg.text())); await page.goto("http://localhost:5173", {waitUntil: "networkidle0"}); await page.screenshot({path: "screenshot.png"}); const mapExists = await page.evaluate(() => !!document.querySelector(".maplibregl-canvas")); console.log("Canvas exists:", mapExists); await browser.close(); })();

import puppeteer from "puppeteer";(async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto("http://localhost:5173", {waitUntil: "networkidle0"}); await new Promise(r => setTimeout(r, 4000)); const features = await page.evaluate(() => { if (!window.m) return "no map"; return window.m.queryRenderedFeatures().map(f => f.layer.id); }); console.log("Layers with rendered features:", [...new Set(features)]); await browser.close(); })();

import puppeteer from "puppeteer";(async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto("http://localhost:5173", {waitUntil: "networkidle0"}); const canvasSize = await page.evaluate(() => { const canvas = document.querySelector(".maplibregl-canvas"); if (!canvas) return null; return { w: canvas.width, h: canvas.height, cw: canvas.clientWidth, ch: canvas.clientHeight }; }); console.log("Canvas Size:", canvasSize); await browser.close(); })();
